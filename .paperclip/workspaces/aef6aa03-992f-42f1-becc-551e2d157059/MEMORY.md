# CEO Memory

- This workspace root is the active `AGENT_HOME`.
- CEO persona files live in `agents/ceo/`.
- Durable personal memory lives under `life/`, `memory/`, and `MEMORY.md` at the workspace root.
- Shared roadmap artifacts for this repo belong in `doc/plans/`.
- When a task explicitly asks for planning in Paperclip, also update the issue `plan` document.
- The active company is Livestream-Copilot (`LIV`); the linked product repo currently frames the product as FluxBoard.
